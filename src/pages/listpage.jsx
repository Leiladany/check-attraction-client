import { Stack } from "@mui/joy";

export const ListPage = () => {

  return (
    <Stack sx={{ gap: { xs: 1, lg: 3 } }}>
      <Stack sx={{ alignItems: "center", mt: 8 }}>

      </Stack>
    </Stack>
  );
};
